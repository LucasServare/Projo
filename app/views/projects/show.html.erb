<table>
  <tr>
    <th> Project Name </th>
    <th> Owner </th>
  </tr>
    <tr>
      <td><%= @project.name %></td>
      <td><%= User.find(@project.creator_id).name %> </td>
      <td>
        <%= link_to("Edit", {:action => 'edit', :id => @project.id}) %>
      </td>
    </tr>
  </table>
<br>

<br>
Users invited to this project:
<br>
<br>
<table>
  <th> Project Users </th>
<% @project.users.each do |user| %>
<tr><td>
  <%=  user.name %>
</td></tr>
<% end %>
</table>

<br>
<br>
<br>
<%= form_for(:project, :url => {:action => 'add_user', :id => @project.id}) do |f| %>

  Want to add a user to this project?
  <br>
  <br>
  Enter their email: <%= email_field_tag(:email, "email@example.com") %>
  <br>
  <br>

<%= submit_tag("Add this user", class: "btn btn-default") %>
<% end %>
