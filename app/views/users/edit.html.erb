Your settings:

<br>
<%= form_for(:user, :url => {:action => 'update', :id => @user.id}) do |f| %>
  <table>
    <tr>
      <th>First Name</th>
      <td><%= f.text_field(:first_name) %></td>
    </tr>
    <tr>
      <th>Last Name</th>
      <td><%= f.text_field(:last_name) %></td>
    </tr>
    <tr>
      <th>Email</th>
      <td><%= f.text_field(:email) %></td>
    </tr>
  </table>

    <br>
    <%= submit_tag("Update User", class: "btn btn-default") %>
    <br>

<% end %>
<br>
<%= form_for(:user, :url => {:action => 'update_password', :id => @user.id}) do |f| %>
  <table>
      <th>Current Password</th>
      <td><%= f.password_field(:current_password) %></td>
    </tr>
    <tr>
      <th>New Password</th>
      <td><%= f.password_field(:password) %></td>
    </tr>
    <tr>
      <th>Confirm Password</th>
      <td><%= f.password_field(:password_confirmation) %></td>
    </tr>
  </table>
  <br>
    <%= submit_tag("Update Password", class: "btn btn-default") %>

<% end %>

<br>

<%= link_to "Prefer to delete your account?", action: "delete", :id => @user.id %>
